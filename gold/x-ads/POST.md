# X picture post — GIF with the examples

**Use this:** [`gold/forms/examples.gif`](../forms/examples.gif)

Same Texas notebook. BEFORE, then infographic / svg / 3js (each svg and 3js is two frames so the loops read), four LOOP close-ups (weenie breathe, river flow, pin ping, quiet tick), then simulation. 720×900.

Local copy for attach tools: `before-after.gif` (written by `python gold/forms/make_gif.py`).

Do not use crop-through `08` / `17` / `18`. No video.

**Alt text:** Same naive Texas notebook HTML, then /reimagine-it infographic, svg, 3js, four micro-motion close-ups, and a year-clock simulation. Loops.

Copy:

```
Same HTML. Alive by default.

/reimagine-it infographic
/reimagine-it svg
/reimagine-it 3js
/reimagine-it simulation

https://github.com/Kayforkind/reimagine-it
```
