# Gold reverse-demo

Fail is a vibe list (exit 1). Ship is the same context as one JSON artifact (exit 0).

```text
python gold/awe.py          # FAIL
python gold/awe.py --ship   # SHIP → gold/shipped.json
```

Open `index.html` for the same two beats without Python. Note that drove it: reverse demo.

Smoke test from repo root: `./gold/test_awe.ps1`.
